{
    "require": {
        "php": "^8.2",
        "ext-json": "*",
        "ext-curl": "*",
        "mikecao/flight": "^3.12",
        "symfony/dotenv": "^7.1",
        "twig/twig": "^3.14"
    },
    "autoload": {
        "psr-4": {
            "App\\": "app/"
        },
        "files": [
            "app/helpers.php"
        ]
    },
    "scripts": {
        "clear-views": "rm -rf cache/views",
        "post-install-cmd": [
            "@clear-views"
        ]
    },
    "config": {
        "optimize-autoloader": true,
        "sort-packages": true
    }
}
